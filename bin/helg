#!/usr/bin/env node
const logSymbols = require('log-symbols');
const helg = require('../');

// eslint-disable-next-line no-console
console.log(helg.ere() ? `${logSymbols.success} Yes` : `${logSymbols.error} Nope`);
